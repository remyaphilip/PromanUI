export class Cardmodel {
    constructor(
        listId: number,
        summary: string,
        currentTimeStamp: Date,
        description?: string,
        startDate?: Date,
        endDate?: Date,
        assignedTo?: number

    ) {

    }
}
